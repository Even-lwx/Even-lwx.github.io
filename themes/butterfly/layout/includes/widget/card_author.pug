if theme.aside.card_author.enable
  .card-widget.card-info.aside-profile-card
    .is-center
      if theme.aside.card_author.kicker
        .aside-profile__kicker
          i.fas.fa-code
          span= theme.aside.card_author.kicker
      .avatar-img
        img(src=url_for(theme.avatar.img) width='110' height='110' loading='lazy' decoding='async' onerror=`this.onerror=null;this.src='` + url_for(theme.error_img.flink) + `'` alt=config.author + ' 的头像')
      .author-info__name= config.author
      if theme.aside.card_author.role
        .aside-profile__role= theme.aside.card_author.role
      .author-info__description!= theme.aside.card_author.description || config.description

    if theme.aside.card_author.skills
      .aside-profile__chips
        each skill in theme.aside.card_author.skills
          span.aside-profile__chip= skill

    .card-info-data.site-data.is-center
      a(href=url_for(config.archive_dir) + '/')
        .headline= _p('aside.articles')
        .length-num= site.posts.length
      a(href=url_for(config.tag_dir) + '/')
        .headline= _p('aside.tags')
        .length-num= site.tags.length
      if site.categories.length > 0
        a(href=url_for(config.category_dir) + '/')
          .headline= _p('aside.categories')
          .length-num= site.categories.length

    if theme.aside.card_author.button.enable
      a#card-info-btn(href=theme.aside.card_author.button.link)
        i(class=theme.aside.card_author.button.icon)
        span=theme.aside.card_author.button.text
